language: generic
sudo: required

services: 
  - docker

before_install:
  - docker build -t kealanmaas/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true kealanmaas/docker-react npm run test
